# ShopAssist Agent 端到端评测 — 失败案例报告

> 评测口径：N=50 题（31 道图查询业务题 + 19 条路由/拒答边界），每题跑 3 轮 = 150 条独立轨迹；
> 金标答案逐条回 Neo4j 实测核验，判分采用「规则初筛 + LLM 全文判读」，全程落盘留痕。分层判定：**路由 → 选工具 → 执行 → 答案命中**。
> 数据凭据：`llm_backend/logs/eval_suite.jsonl`（150 行）、`llm_backend/eval_suite.py`（50 题）。

## 总指标（严格口径，A6「只反问型号未编价」不算过）

| 层 | 指标 | 值 |
|---|---|---|
| 路由 | 判型准确率 | **95%**（57/60 轮批测） |
| 执行 | 工具无异常 | **99.3%**（149/150 轮） |
| 端到端 | pass@1（首轮答对） | **58%**（29/50） |
| 端到端 | pass@3（3 次内至少 1 次答对） | **80%**（40/50） |
| 端到端 | 3 轮全过一致性 | **50%**（25/50） |

**关键信号**：路由、执行两层接近满血（95% / 99.3%），但端到端 pass@1 只有 58%。
→ 失败不是「系统崩了」，而是集中发生在**答案命中**这一层：判型对了、检索到了，最终却答错或答不准。
pass@1 与 pass@3 的落差（58% → 80%）进一步说明：失败以**可重试修复的抖动**为主，而非系统性能力缺失。

---

## 失败模式分类（按层归因）

### 层 2 检索/工具：预定义模板字段不全
- B3「订单 93 的员工是谁」→ 答成「客户」；B4「哪家物流配送」→ 答「查不到」；B18「孙凤英处理多少单」→ 0/3 无覆盖。
- 根因：白名单模板 `order_by_id` 只 JOIN 了商品/客户，没 JOIN Employee / SHIPPED_VIA；「员工处理量」无对应模板。
- 修法：补 order 相关模板的员工、物流、数量字段；新增「员工处理量」模板。

### 层 2 检索/工具：中文姓名反序（数据缺陷，真 bug）
- 姓名库存成「凤英 孙」（姓在 LastName 列），模板拼 `FirstName + ' ' + LastName` =「凤英 孙」；用户说「孙凤英」时 `CONTAINS '孙凤英'` 永远匹配不上。
- 后果：B3 / B18 员工类系统性失败，与「模板字段不全」叠加。
- 修法：按中文习惯拼 `LastName + FirstName`，或改 `CONTAINS '凤英'`。

### 层 3 执行：一处工具中止（text2cypher 参数不传）
- B3r2 触发 text2cypher 生成带 `$employee_name` 参数，但执行节点 `graph.query(statement)` 不传参 → 中止（harness 记为 FAIL，未中断整批）。
- 修法：生成/修正提示强制「禁止 `$` 参数、值内联」；执行层加参数注入。

### 层 4 答案/总结：统计与多跳语义漂移
- B12「2023-10 多少笔订单」→ 答成销售额（未给「5 笔」）；
- B17「哪个产品评分最高」→ 答「格力 3.37」（模板按**评价数**排，把「评分最高」和「评价最多」混了；真值是华为 开关 Mini 均分 4.45）；
- B28「多少个品类」→ 幻觉编「20 个」（真值 8，还把库里没有的音箱/门锁/扫地机/净水器列上）；
- B21「哪些订单延迟发货」→ 只答 3 笔，漏 46/68（真值 5 笔）；
- B11「格力均分」→ 说「无评分」（实有 7 条均 3.37）。
- 根因：count/sum/avg/top-N 类需要「先聚合再归因」，当前 summarize 把单条记录当答案，或对聚合语义理解漂移。
- 修法：聚合类问题走专门 Cypher + 明确「求和 ≠ 计数」的少样本示例。

### 层 4 答案：空结果回避（不敢肯定「无」）
- B25「有没有库存 <10 的产品」→ 真值是「没有」，三轮答「查不到 / 可能没更新，建议问仓库」，未如实给否定结论。
- 根因：空结果被当成「查询失败」，而非「合法答案 = 无」。
- 修法：区分「查询异常」与「空结果」，空结果时明确输出「没有」。

### 层 4 答案：拒售幻觉不稳定（out-of-catalog 边界）
- A4「卖运动鞋吗」→ 幻觉「有卖鞋」；A14「智能扫地机器人」→ 三轮「有的哦，要什么价位」（库里没有）；
- 对照 A8「智能手表」→ 正确说「没有」。
- 根因：同一类「无此品」问题，LLM 自由发挥，答案在「有 / 没有」之间随机。
- 修法：拒答改成「先查 catalog 有没有再答」，没查到一律走拒答模板，不让 LLM 自由发挥。

### 层 4 答案：兜底话术过宽（答非所问）
- 兜底句「没这类商品去别家」被错误接走：A11 开发票、A13 物流查询、A17 退货运费、A19 取消订单。
- 根因：这些「售后/物流/发票」问题本应「要单号」或「引导售后」，却被兜底话术兜住。
- 修法：收窄兜底触发条件，售后类问题单独路由到「要单号 / 引导」分支。

### 层 1 路由：过度澄清（该全局答却反问单号）
- B6「顺丰配送了哪些订单」→ 反问「请补全单号」；B31「订单 13 什么时候下单」→ 三轮都反问，题面已给「订单 13」；B16 / B19 / B21 同款。
- 根因：路由 prompt 对「缺单号」触发过敏感，把「全局集合题」「已给单号题」误判成 additional。
- 修法：路由判定加「题面已含完整订单号则不再追问」的规则 / 少样本。

### 附：检索命中但 summary 谎称「无」（总结节点信息丢失）
- B6 检索其实命中顺丰 9 单，但 summary 说「无法区分 / 无信息」；B8 / B14 价格题 r2/r3 谎「未上架 / 未查到」（数据在库）。
- 根因：summarize 节点对检索结果做二次生成时丢失信息。
- 修法：总结前把检索到的结构化结果原文注入提示，强制「结果非空则必须引用」。

---

## 结论（面试怎么讲）

一句话：**系统没有崩——路由和执行层是稳的（95% / 99.3%），失败集中在「最后一公里」答案命中层**，
而且 8 类失败都能归因到「模板缺字段 / 数据反序 / prompt 边界 / 聚合语义」这些**已知、可修**的点，没有一类是「无解的黑盒」。

这反而是加分项：面试官问「你系统哪里做得不好」，能拿出 50 题逐题人工核的失败清单 + 根因 + 修法，比「全绿」可信得多。
