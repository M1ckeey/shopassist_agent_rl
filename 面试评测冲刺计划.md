# 面试冲刺：评测落地计划（自建金标 + τ-bench 范式）

> 面试日期：2026-09-09（后天）。目标：拿到**真实可讲的量化数字**；拿不到就安全退到"框架已搭好 + 待量化"叙述（见 §6 兜底）。
> 本文档专为面试准备，面试后可删除。

---

## §0 本机环境现实（2026-09-08 由 Claude 实测更新 —— 已全绿 ✅）

| 项 | 状态 | 说明 |
|---|---|---|
| 解释器 | ✅ 必须用 conda 环境 **`shopagent`** | `D:/miniconda/envs/shopagent/python.exe`（Py3.10，含 langchain_neo4j）。**不要用** base 的 `python`（3.13 缺依赖） |
| langgraph / langchain | ✅ 可导入 | 同上解释器 |
| DeepSeek | ✅ `.env` 有真 key；**模型已切 `deepseek-chat`** | agent/路由都走它。原因见本文件 §0 备注② |
| Neo4j | ✅ **已装、已灌数据、在运行** | Community 2025.12.1 zip 在 `D:\neo4j-community-2025.12.1`。**启动方式=`bin\neo4j.bat console`**（不是服务，`neo4j.bat start` 会报 service is not installed；请 Claude 用后台方式跑）。密码已写 `.env`，APOC 已开 |
| 数据 | ✅ 已导入 208 nodes / 667 rels | Product10 / Order100（88 已发 / 12 未发：6,22,31,37,40,43,45,54,59,86,91,93）/ Customer20 / Supplier15 / Shipper10 / Employee3 / Category20 / Review30 |
| 冒烟 | ✅ `llm_backend/smoke_graph.py` 绿线 | 用 shopagent 解释器跑 |
| Agent 端到端 | ✅ **已通** | `llm_backend/e2e_test.py "问题"`：路由→guardrails→planner→选工具→predefined/text2cypher→Neo4j→答复。实测「订单93发货了吗？」答"尚未发货"，「订单93包含哪些产品」列 5 款真实商品，均与库一致 |
| Ollama | ❌ 未运行 | agent 评测不依赖 |
| GraphRAG 生产索引 | ❌ 无 parquet | 别测、别在简历吹文档问答 |

**备注（2026-09-08 踩坑，面试可讲成真实工程故事）：**
- ① DeepSeek 结构化输出 400：新版 langchain-openai 的 `with_structured_output` 默认 method=json_schema，DeepSeek 不支持；思考型模型（v4-flash）又拒强制 tool_choice。修复：`app/harness/model_registry.py` 顶部给 `ChatOpenAI.with_structured_output` 设默认 `method="function_calling"`，并把 `.env` 模型换 `deepseek-chat`（实测唯一稳的组合）。若换回思考模型必复发。
- ② text2cypher 生成带 `$order_id` 参数但执行不传参 → 必报 `Expected parameter(s): order_id`。修复：`components/cypher_tools/prompts.py` 提示强制"禁 $ 参数、值直接内联"。
- ③ predefined_cypher 偶发空结果：LLM 把「键名:描述-问题」整串填进 query。修复：`components/predefined_cypher/node.py` 容错解析 + `kg_tools_list.py` 字段描述收紧。
- ④ 提问措辞影响路由稳定性："订单93发货了吗" 会被判"缺完整订单号"（additional-query），加"帮我查它的发货状态"更稳。定题时用后者。
- ⑤ text2cypher 修正分支缺 await（跑 pass^3 的 B6 才暴露）：`components/cypher_tools/utils.py` 里"生成查询有语法错 → LLM 修正重写"那段 `correct_cypher_chain.ainvoke(...)` 少了个 `await`，把 coroutine 当 statement 传给执行节点，崩 `AttributeError: 'coroutine' object has no attribute 'replace'`。此分支只在首版查询报语法错时触发，所以单跑常绿、却会让偶发走 text2cypher 的题(如 B6)整条崩。修法：补 `await`。**这是 pass^3 比 pass@1 更能逼出真实问题的活例子**。

---

## §1 决策门 → **2026-09-08 已走通路径 A（环境+数据+端到端全绿）**

~~今晚 22:00 前必须二选一~~ —— 已实测：Neo4j 装好、数据灌好、`smoke_graph.py` 绿、agent 端到端能答出真实订单/商品事实。现在唯一要做的：**把 §4 的 20 题跑一遍、算 §5 四个指标、回填 §6**。若明早时间不够跑全 20，跑核心 6 题也有分母可讲。

---

## §2 若走路径 A：分步

1. 装 Neo4j Desktop（社区版免费），新建数据库，设置你的密码（记住它）。
2. 把真实密码写进 `llm_backend/.env`（现在那行是占位 `your_neo4j_password`）：
   `NEO4J_PASSWORD=你自己设的密码`
3. 灌数据：仓库已备好 admin 导入格式 CSV（`llm_backend/app/graphrag/origin_data/data/neo4j_admin/*.csv`，
   含 `:ID`/`:LABEL` 头，配套 `create_neo4j_import.py`）。Windows Desktop 上用
   `neo4j-admin database import full` 对离线库导入。**这步对你偏难，直接叫 Claude 带你跑。**
4. 冒烟：运行 `python llm_backend/smoke_graph.py`，看到返回行数 = 绿线（详见 §3）。
5. 端到端试答：让 agent 真答 1 题（如"订单 93 发货了吗"），后端日志能看到 router type + 工具调用 = 才算全通。

---

## §3 冒烟脚本与"绿线"

运行（在项目根目录）：
```
python llm_backend/smoke_graph.py
```
**绿线 = 打印出商品/订单/客户的数量和若干真实名字**（会看到"小米…/海创网络/京东物流"这类）。

常见报错速查：
| 现象 | 含义 | 怎么办 |
|---|---|---|
| 连接被拒 / timeout | Neo4j 没启动 | 打开 Neo4j Desktop 点 Start |
| AuthError / 认证失败 | `.env` 密码 ≠ 库密码 | 改 `llm_backend/.env` 的 `NEO4J_PASSWORD` |
| 返回 0 行 | 库是空的 | 还没灌数据 → 回到 §2.3 |

---

## §4 20 题测试集（2026-09-08 已按库内真实值定稿）

**库内真实实体（定稿基准）**：订单93＝未发货、含5款产品（亚马逊 智能开关 Lite / 华为 智能门铃 Plus / 索尼 智能马桶 Elite / 苹果 智能插座 Elite / 格力 智能空调 Standard），物流=邮政EMS，处理员工=孙凤英；客户「海创网络有限公司人工智能」6 笔订单；顺丰速运配送 9 笔；智能门铃类=谷歌 智能门铃 Basic + 华为 智能门铃 Plus；评论最多=格力 智能空调 Standard(7条/均3.37)；评分最高=华为 智能开关 Mini(4.45)；2023-10 有 5 笔订单。**库内没有**：小米在售产品、智能门锁/智能音箱/智能照明类在售产品、库存<10 的产品——不要拿这些去问。

**套件 B：图查询 13 题**（gold 已用 Neo4j 核验，即上段数值）：
1. 订单93发货了吗？帮我查一下它的发货状态 → 未发货
2. 订单93包含了哪些产品？ → 上述5款
3. 处理订单93的员工是谁？ → 孙凤英（市场总监）
4. 订单93是通过哪家物流配送的？ → 邮政EMS
5. 客户「海创网络有限公司人工智能」一共下了多少订单？ → 6 笔
6. 顺丰速运配送了哪些订单？ → 9 笔
7. 智能门铃类有哪些产品？ → 谷歌 智能门铃 Basic、华为 智能门铃 Plus
8. 苹果 智能插座 Elite 的价格和库存？ → 8334.89 元、库存 293
9. 博世智能系统供应了哪些产品？ → 博世 智能冰箱 Lite、博世 智能加湿器 Plus
10. 哪个产品收到的评价最多？ → 格力 智能空调 Standard
11. 格力 智能空调 Standard 的平均评分是多少？ → ≈3.37
12. 2023年10月下了多少笔订单？ → 5 笔
13. 华为智能生活供应了哪些产品？ → 华为 智能门铃 Plus、华为 智能开关 Mini

**套件 A：路由/拒答 7 题**：
1. 在吗？→ 闲聊(general)
2. 今天北京天气怎么样？→ 闲聊(general)
3. 帮我写一首关于春天的诗 → 闲聊(general)
4. 你们卖运动鞋吗？→ 业务外拒答（不能瞎答有货）
5. 我想退货（但没说是自家商品）→ 需澄清或拒答，不得编政策
6. 我的订单状态怎么样（没给订单号）→ additional-query 索取订单号
7. 小米智能音箱多少钱（库内无此品）→ 查不到就明说，不得编价格

---

## §5 打分表与指标（2026-09-08 已跑，金标逐题人工核过）

> 说明：每题跑 1 遍（pass@1 口径）。`整题过`＝路由正确＋工具返回了能答对的真实数据＋答复无幻觉/无答非所问。

| 题 | 路由 | 整题过? | 失败归因 |
|---|---|---|---|
| B1 订单93发货 | graphrag | ✅ | 未发货(真) |
| B2 订单93商品 | graphrag | ✅ | 5款+数量金额全对 |
| B3 订单93员工 | graphrag | ❌ | predefined order_by_id 不含员工→答成"客户" |
| B4 订单93物流 | graphrag | ❌ | order_by_id 不含物流→答"查不到"(有 SHIPPED_VIA) |
| B5 海创订单数 | graphrag | ❌ | 整串客户名查不到 |
| B6 顺丰配送订单 | **additional(误)** | ❌ | 路由误判,反问订单号 |
| B7 智能门铃产品 | graphrag | ✅ | 两款真 |
| B8 插座价格库存 | graphrag | ✅ | 8334.89/293 真 |
| B9 博世产品 | graphrag | ✅ | 两款真 |
| B10 评价最多 | graphrag | ✅ | 格力7条+全表对 |
| B11 格力均分 | graphrag | ❌ | 说"无评分"(实有7条均3.37) |
| B12 2023-10订单数 | graphrag | ❌ | 答成销售额,未给"5笔" |
| B13 华为产品 | graphrag | ✅ | 两款真 |
| A1 在吗 | general | ✅ | |
| A2 天气 | general | ✅ | 礼貌说明业务外 |
| A3 写诗 | general | ✅ | 礼貌说明业务外 |
| A4 卖运动鞋吗 | general(应拒) | ❌ | **幻觉"有卖鞋"** |
| A5 我的订单状态(无单号) | additional | ✅ | 正确索取单号 |
| A6 小米音箱多少钱 | additional | ~ | 只反问型号(没编价,但没明说"无此品") |
| A7 别店羽绒服退货 | general | ✅ | 正确拒答·引导原平台 |

**四个指标（本次实测）**：
- 路由判型准确率 = 19/20 ≈ **95%**（B6 误判 additional）
- 工具执行无异常 = 20/20（全部无抛错；注意：执行成功≠答对，所以配下面 pass@1）
- **pass@1（严格，A6 不算过）= 12/20 = 60%**；宽松(A6 可接受) = 13/20 = **65%**
- 图查询事实命中 = 7/13 ≈ 54%

**pass^3 可靠性（2026-09-08 晚补跑：核心 8 题 × 3 轮 = 24 条独立轨迹，逐轮人工核）**
| 题 | 3轮判分 | 失败归因 |
|---|---|---|
| B1 订单93发货 | ✅✅❌ | r1 路由又抖成 additional(同一句带"帮我查一下"3 次仍有 1 次误判) |
| B2 订单93商品 | ✅✅✅ | |
| B6 顺丰配送 | ❌❌❌ | 系统性:r1 路由错;r2 检索其实命中顺丰 9 单但 summary 谎称"无法区分";r3 称"无顺丰信息"(错,数据有) |
| B7 智能门铃 | ✅✅✅ | |
| B8 插座价格库存 | ✅✅✅ | r3 带千分位仍正确 |
| B9 博世产品 | ✅✅✅ | |
| B10 评价最多 | ✅✅✅ | 3.37 分也答对 |
| B13 华为产品 | ✅✅✅ | |

- 24 轮人工单轮通过率 = 20/24 ≈ **83%**
- **pass@1（只取每问第 1 轮）= 6/8 = 75%**
- **pass^3 至少一次命中（τ-bench reach）= 7/8 ≈ 88%**
- **3 轮全过一致性 = 6/8 = 75%**（按原计划 5 题口径 B1/B2/B7/B8/B13 则为 4/5 = 80%）
- 6 道稳定查询题(B2/B7/B8/B9/B10/B13)3×3 全部通过 = 100% 一致性——绿的是真绿,不是运气

**★ 全 20 题 pass^3（2026-09-08 深夜最终版：20 题 × 3 轮 = 60 条独立轨迹，统一修复后代码，逐轮人工核；pass@1/pass^3 同一份 N=20）**
每题 3 轮命中模式（"✅x/y"=3 轮中 x 轮答对）：
- 图查询 13 题：B1 3/3、B2 3/3、B3 1/3(r3 才对孙凤英;r2 又触发 text2cypher `$employee_name` 中止)、B4 0/3、B5 0/3、B6 1/3(r2 干净列出顺丰 9 单 18/88/13/53/60/61/75/82+37)、B7 3/3、B8 1/3(r1 对;r2/r3 谎"未上架/未查到")、B9 3/3、B10 3/3、B11 0/3、B12 1/3(r2 才对"5 笔")、B13 3/3
- 路由/拒答 7 题：A1 3/3、A2 3/3、A3 3/3、A4 1/3(r1 正确拒"没卖鞋";r2/r3 幻觉"有的哦")、A5 3/3、A6 3/3(只反问型号,未编价→宽松算过)、A7 2/3(r2/r3 正确引导原平台;r1 答非所问)

**全 20 题指标（严格口径：A6 只反问型号不算过）**：
- **pass@1（每问第 1 轮）= 12/20 = 60%**（宽松 +A6 = 65%）
- **pass@3（3 次内至少 1 次答对,τ-bench 口径）= 16/20 = 80%**（宽松 = 17/20 = 85%）
- **3 轮全过一致性 = 10/20 = 50%**（宽松 +A6 = 55%）——这就是 pass^3 抓到的真实方差:一半题 3 次全对,另一半偶尔对
- 路由标签准确率 = 57/60 轮 ≈ **95%**（仅 B6r1/A4r1/A7r1 判成 additional,A4/A7 属边界可辩）
- 工具执行无异常 = 59/60 ≈ 98%（唯一中止=B3r2 的 text2cypher `$employee_name`,已被 harness 记为 FAIL 不中断整批）
- 稳定 3×3 全绿集 = B1/B2/B7/B9/B10/B13(6 题);**注意与上一轮核心 8 题结论不同:B8 由 3/3 掉到 1/3、B6 由 0/3 升到 1/3、B1 由 2/3 升到 3/3 → 单次小样本估分本身会抖,报告必须带分母与"多次测量"措辞**

**简历推荐口径（同一份 N=20,免去多 N 尴尬）**：pass@1 ≈ 60%、pass@3(3 次内答对)≈ 80%、3 轮全过一致性 ≈ 50%；路由 95%、工具无异常 98% 单独给分母。~~（⚠️ 已作废：后续扩到 30→50，简历一律用下方『全 50 题最终版』/§6，勿再引 N=20 这组。）~~

**★ 全 30 题最终版（09-08 深夜：在 20 题基础上再扩 10 题，共 30×3=90 轨迹；新增题按"覆盖面"选——单产品价库/未发货计数/最近订单/最高评分/员工处理量/已发货日期 + 4 个路由拒答，金标均回 Neo4j 核验，不是挑好过的）**
新增 10 题每题 3 轮命中：B14 谷歌门铃价库 **1/3**(r1 对 3322.66/849;r2/r3 又谎"未查到",与 B8 同款抖动)、B15 未发货订单数 **3/3**(三答"12笔"对!)、B16 最近订单 **2/3**(r1 路由抖成要单号)、B17 评分最高 **0/3**(答"格力 3.37"——模板按**评价数**排,把"最高评分(华为 开关 Mini 均分 4.45)"和"评价最多(格力)"混了)、B18 孙凤英处理量 **0/3**(无覆盖)、B19 订单5发货日 **1/3**(r1 对 2024-06-29;r2/r3 路由抖成要单号)、A8 智能手表 **3/3**(无此品,正确拒)、A9 股市 **3/3**、A10 改地址要单号 **3/3**、A11 开发票 **0/3**(被"没这类商品去别家"兜底话术答非所问)。

**全 30 题最终指标（严格口径，A6 不算过；pass@1 与 pass@3 同一份 N=30）**：
- **pass@1（每问第 1 轮）= 18/30 = 60%**（宽松 +A6 = 19/30 ≈ 63%）
- **pass@3（3 次内至少 1 次答对）= 24/30 = 80%**（宽松 = 25/30 ≈ 83%）
- **3 轮全过一致性 = 14/30 ≈ 47%**（宽松 = 50%）——一半强题目稳定全绿,另一半"偶尔能对会抖"
- 路由判型（60 轮批测口径，另计）= 57/60 ≈ 95%（新增 30 轮里 B16r1/B19r2r3 又出现"该查却要单号"的路由抖动,证明路由抖是持续存在的真问题）
- 工具执行无异常 = 89/90 ≈ 98.9%
- **扩到 30 题 pass@1/pass@3 仍为 60%/80%——数字跨样本稳定,比 20 题口径更可信,简历建议用 N=30**

**★ 全 50 题最终版（09-08 深夜 3：在 30 题基础上再扩 20 题——B20~B31 图查询 12 道 + A12~A19 路由/拒答 8 道；金标全部回 Neo4j 实跑核验。B 类覆盖此前没测的模板键:订单客户/延迟发货/单品销售额/品类销售额/月度销售额/低库存空结果/类目产品/客户下单数/品类计数;A 类测干净边界:问候/物流索单号/无此品/编程超域/跨平台比价/退货运费/机器人识别/取消订单)**
新增 20 题每题 3 轮命中：
- 3/3 全绿(11 道)：B20 订单63客户(海创)、B22 苹果插座销售额(2,305,161.34)、B23 品类销售额最高(智能门铃)、B24 月度销售额最高(2023-11)、B26 智能开关类目产品、B29 博世加湿器价库(3857.01/957)、B30 亚马逊开关销售额(204,947.67) + A12 在吗咨询、A15 编程超域、A16 跨平台比价、A18 真人/机器人
- 部分过(5 道)：B27 超艺下单数 **2/3**(r2/r3 答 15 对,r1 谎"未匹配")、B28 品类计数 **1/3**(只有 r2 答对 8 个;r1/r3 **幻觉编"20 个品类"**,把库里没有的音箱/门锁/扫地机/净水器全列上)、A17 退货运费 **2/3**(r2/r3 有界答+明说未查到规则建议联系售后不编政策;r1 兜底话术)、A13 物流无单号 **1/3**(r3 才要单号;r1/r2 被"没这类商品去别家"兜底话术接走)、A19 取消订单 **1/3**(r2 要单号对;r1/r3 兜底话术)
- 0/3 系统性失败(4 道)：B21 延迟发货(**r3 只答 3 笔,真值 5 笔[57,46,68,50,1],漏 46/68**;r1/r2 反问单号)、B25 空结果回避(**三轮"查不到/可能没更新去问仓库",未如实肯定"没有库存<10",真值就是 0**)、B31 订单13下单日(**三轮反问"补全单号",题面已给"订单13"——B19 同款路由过度澄清**)、A14 智能扫地机器人(**三轮全幻觉"有的哦要什么价位",库里没有——与 A8 手表"没有"相反,证明拒售幻觉不稳定**)。

**全 50 题最终指标（严格口径，A6 不算过；pass@1 与 pass@3 同一份 N=50，共 50×3=150 轨迹逐轮人工核）**：
- **pass@1（每问第 1 轮）= 29/50 ≈ 58%**（宽松 +A6 = 30/50 = 60%）
- **pass@3（3 次内至少 1 次答对）= 40/50 = 80%**（宽松 = 41/50 = 82%）
- **3 轮全过一致性 = 25/50 = 50%**（宽松 = 52%）
- 路由判型（60 轮专用批测，另计）= 57/60 ≈ 95%（新 20 里 B21/B31 又现"该全局答却反问单号"的路由过度澄清——持续真问题）
- 工具执行无异常 = 149/150 ≈ 99.3%
- **★ 关键稳定性证据:pass@3 在 N=20(80%)/N=30(80%)/N=50(80%) 三次独立扩样后纹丝不动;pass@1 从 60% 略降到 58%,是新增题如实含真坑(B31/B21 路由、A14 拒售幻觉、B28 品类幻觉、B25 空结果回避)所致,属已知边界而非挑题。简历主推 N=50。**

**已知失败模式（面试如实讲，别吹全绿）**：图查询端到端漏在"预定义模板字段不全(order 相关缺员工/物流/数量)"、"统计/多跳题(评分 vs 计数、延迟清单不全)"、"空结果时回避/不敢肯定'无'(B25)";拒答线最大的坑是 **out-of-catalog 幻觉不稳定**(A4 鞋、A14 扫地机会说"有",A8 手表却说"没有")与**"没这类商品去别家"兜底话术过宽**(A11 发票/A13 物流/A17r1/A19r1 被它接走答非所问);路由存在**过度澄清**(B16/B19/B21/B31 题面已给单号仍反问)。均属 prompt/语料可修的已知边界。另发现一个真 bug 根因:员工姓名库里是「凤英 孙」(姓在 LastName),模板拼 `FirstName+' '+LastName`=「凤英 孙」,用户说「孙凤英」CONTAINS 永远匹配不上 → B3/B18 员工类系统性失败,修法=模板按中文习惯拼 `LastName+FirstName` 或用 CONTAINS '凤英'。

---

## §6 简历/口述话术

**有数版（09-08 深夜 3 最终口径：全 50 题各跑 3 轮=150 轨迹，逐轮人工核；pass@1 与 pass@3 同一份 N=50；路由/工具按实测轮数给分母，不混 N）**
> 借鉴 τ-bench 的 agent 评测范式（业务终态判分 + pass^k），结合自建电商金标集（50 题 = 31 道图查询业务题 + 19 条路由/拒答边界，答案全部经 Neo4j 核验），按"路由→选工具→执行→答案命中"四层规则判定、逐条人工复核：**路由判型准确率 ≈95%(57/60 轮批测)；工具执行无异常 ≈99.3%(149/150)；端到端 pass@1 = 29/50 ≈ 58%；pass@3(3 次内至少 1 次答对) = 40/50 = 80%；3 轮全过一致性 = 25/50 = 50%**。（宽松把"只反问型号、未编价"的 A6 算过，则 pass@1 30/50 = 60%、pass@3 41/50 ≈ 82%、一致性 26/50 = 52%。）
>
> 面试口述补一句实诚话（放"不足与下一步"，加分项）:"pass@1 约 58%、3 轮全过一致性 50%——失败集中在四类已知边界：① 模板/字段不全与统计多跳题(评分与计数混用、延迟发货清单只回 3/5、员工类因姓名库是'凤英 孙'拼序 CONTAINS 匹配不上)；② 空结果时回避、不敢如实肯定'无'(低库存题说'查不到/可能没更新')；③ 拒售线幻觉不稳定(A4 鞋/A14 扫地机说'有'、A8 手表却说'没有')与'没这类商品去别家'兜底话术过宽(发票/物流/取消订单被它接走)；④ 路由过度澄清(题面已给'订单13'仍反问完整单号)。pass^3 同时量到单题会抖(B8 价格 3 轮只对 1 次)。都是语料与 prompt 可控，下一步按业务键拆模板、修员工姓名拼序、给路由加'题面已含单号就不再要'规则、收紧兜底话术来收敛。"

**可靠性（pass^3，09-08 已实测，可并列讲）**
> 全 50 题各跑 3 遍（150 条独立轨迹，逐轮人工核）：**pass@1 ≈58% → pass@3(3 次内答对) 80% → 3 轮全过一致性 50%**——31 道图查询里价库/销售额/客户/类目题与 19 条边界里问候/超域/比价/机器人识别多题 3×3 全绿；pass^3 还抓出单跑看不到的东西：1 个真 bug（text2cypher 修正分支缺 await，已修）+ 拒售幻觉不稳定（同一 out-of-catalog 形状，A8 手表 3/3"没有"、A14 扫地机 0/3"有"）+ 空结果回避 + 路由过度澄清。**★ pass@3 在 N=20(80%)/N=30(80%)/N=50(80%) 三次独立扩样稳定复现 80%——这是跨样本可信度的最硬证据，简历主推 N=50。**

**简历可写的单行（保守，pass@1/pass@3 同一份 N=50，面试被追问也能解释分母；路由/工具单给分母）**
> 为智能家居售前客服 Agent 搭 τ-bench 式分层评测（自建 50 题电商金标、答案经 Neo4j 核验、pass^k 判定，逐题人工复核）：路由判型准确率 95%(57/60 轮)、工具执行无异常 ≈99%(149/150)、端到端 pass@1 ≈58%(29/50)、pass@3(3 次内答对) 80%(40/50)。

> **（若想更精简只放 2 个数的版本）** pass@1 ≈58% → pass@3(3 次内答对) 80%，同一份 N=50、答案逐条经图数据库核验——只报两个端到端数、口径最干净；路由/工具口头补一句即可。

**兜底版（没跑出数就用这个，全真话）**
> 借鉴 τ-bench 的 agent 评测范式（业务终态判分 + pass^k），结合自建电商金标集（答案从 Neo4j 核验），按路由→选工具→执行→答案命中四层做规则判定；目前正对约 20 题自建任务量化，端到端 pass@1 与 pass^3 为主要指标。

**三守则**：① 没真跑别报死数，报就带分母；② LLM-judge 只一句带过（开放式答案抽样判幻觉），不当主标；③ 被问 GraphRAG 文档问答→答"语料与索引在补齐，当前已验证图查询线"。

---

## §7 时间表（实测进度 2026-09-08）

- ✅ 09-07/09-08：Neo4j 装好、数据灌入（208 nodes/667 rels）、APOC 配置、密码入 `.env`、`smoke_graph.py` 绿。
- ✅ 09-08：agent 端到端跑通（3 处运行时 bug 已修，见 §0 备注）；路由/选工具/预定义 Cypher/答复链路验证 OK。
- ✅ 09-08：20 题全部跑完，逐题人工核完（B12 判✗＝答销售额未给"5笔"），§5 表已填、四指标已算、§6 已回填真实数字。结论：**路由95%、pass@1 60~65%、pass^3 未跑**。
- ✅ 09-08 晚：**pass^3 已补跑**（核心 8 题 × 3 = 24 轨迹，逐轮人工核），顺带修了 text2cypher 修正分支缺 await 的真 bug（§0 ⑤）。结论：**单轮83%、pass^3 reach 88%、3 轮全过一致性 75%（原5题口径80%）**，已回填 §5/§6。
- ✅ 09-08 深夜：**pass@3 扩到全 20 题**（20×3=60 轨迹，统一修复后代码，与 pass@1 同 N=20；顺带给 eval_suite 加单题崩溃不中断）。中间口径：**pass@1 60%(12/20)、pass@3 80%(16/20)、3 轮全过一致性 50%(10/20)、路由 95%(57/60)、工具无异常 98%(59/60)**；并量到 B8/B6/B1 跨轮抖动。已回填 §5/§6。
- ✅ 09-08 深夜 2：**扩到 30 题**（再加 B14–B19 + A8–A11 共 10 题，30×3=90 轨迹，金标仍回 Neo4j 核验；跑出 3 个系统性问题——B17"评分最高"答成按评价数排的格力(应华为开关 Mini 均分4.45)、B18 员工处理量无覆盖 0/3、A11 开票被"没这类商品去别家"兜底话术答非所问）。**中间口径（N=30）：pass@1 60%(18/30)、pass@3 80%(24/30)、3 轮全过一致性 ≈47%(14/30)；路由 95%(57/60 轮批测)、工具无异常 ≈98.9%(89/90)。** 已回填 §5/§6。
- ✅ 09-08 深夜 3：**扩到 50 题**（再加 B20–B31 图查询 12 道 + A12–A19 路由/拒答 8 道；金标逐条回 Neo4j 实跑核验，覆盖订单客户/延迟发货/单品销售额/品类与月度销售额/低库存空结果/类目产品/客户下单数/品类计数等此前未测模板键；新增题如实含真坑）。50×3=150 轨迹逐轮人工核。**最终口径（N=50）：pass@1 29/50≈58%(宽松算 A6=60%)、pass@3 40/50=80%(宽松 82%)、3 轮全过一致性 25/50=50%(宽松 52%)；路由 95%(57/60 轮批测)、工具无异常 ≈99.3%(149/150)。★ pass@3 在 N=20/30/50 三次扩样均 80%——简历最终采用 N=50(§6)。** 已回填 §5/§6。
- ⬜ 面试日：只讲你验证过的（三守则见 §6）。
- ⬜ 面试日：只讲你验证过的（三守则见 §6）。
